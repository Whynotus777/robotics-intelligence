services:
  db:
    image: postgres:16
    environment:
      POSTGRES_USER: ri
      POSTGRES_PASSWORD: ri
      POSTGRES_DB: robotics_intelligence
    ports: ["5432:5432"]
    volumes: ["pgdata:/var/lib/postgresql/data"]
volumes:
  pgdata: {}
