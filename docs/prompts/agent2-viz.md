# Agent 2 — visualization components
You are Agent 2 (viz) in `Whynotus777/robotics-intelligence`. Read `AGENTS.md`, `docs/ownership.md`, `docs/prompts/claude-design-v1.md` sections 5.1 and 5.3, and `docs/design/phase1-canvas.html`, which contains a working prototype of the nested-territories Explore (1a), the stack-first challenger (1b), and the MRI layer focus; port those, do not reinvent them. Build in `packages/viz` only, as React components with no data fetching and no routing, props typed from `@ri/api-contracts`: `ExploreTerritories` (deterministic squarified treemap over the `/explore` partition tree, five lenses, size measure, three zoom levels, animated re-layout that keeps mark identity, hover card, `onOpenEntity` callback), `ExploreStackMatrix` (the 1b challenger: lens columns × canonical stack layer rows, cell hover, double-click column expansion), and `RobotMRI` (embodiment-labelled layers from the stack route, muted known-empty layers, omitted non-applicable layers, focus state with the compress-left behaviour, cross-embodiment toggle). Use d3-hierarchy for the treemap and plain SVG/CSS transitions; no force layout. Both Explore components must render from the same `packages/fixtures/generated/explore__*.json` files. Add a `packages/viz/dev` Vite page that mounts each component on fixtures so they can be judged on interaction. Keep `pnpm check` green, push to `main`, report in one paragraph.
