# Agent 1 — web shell + live deploy
You are Agent 1 (web) in `Whynotus777/robotics-intelligence`. Read `AGENTS.md`, `docs/ownership.md`, `docs/data-contract.md`, `docs/prompts/claude-design-v1.md`, and `docs/design/phase1-canvas.html` (the actual design markup and tokens; reproduce its tokens, type scale, grid, and components faithfully). Scaffold `apps/web` as a Next.js 15 App Router app (TypeScript, Tailwind with the design tokens as CSS variables, Geist and Geist Mono via `next/font`). Data access goes only through `DataProvider` from `@ri/fixtures`, defaulting to `FixtureProvider` so the app runs with no database; `DATA_PROVIDER=http` switches to `HttpProvider` later. Build these routes against the generated fixtures: `/` Explore (for now a searchable, filterable list grouped by embodiment plus the "What's changing" strip; the treemap arrives from `@ri/viz` and you will swap it in when it exists), `/r/[slug]` the robot profile exactly as designed in 5.2 (identity block, commercial stage, Explore-from-here strip, How-we-know rail, sections omitted when empty, specs by layer with the quiet evidence glyph), `/r/[slug]/stack` a first MRI (vertical layers from the stack route, focus state on click), `/m/[slug]` and `/t/[slug]` the Market/Task explorer as designed in 5.4 (sector rail, maturity board, task panel with the analyst rationale inline), the Evidence Drawer as a shared right-side drawer opened from any evidence chip using the claim-evidence route, a global ⌘K palette over the search fixtures, and the path bar. Company, Compare, Atlas, Updates are placeholders with the nav present. Respect the existing `apps/web/vercel.json`; make sure a Vercel project with root `apps/web` builds from this monorepo (pnpm, `outputFileTracingRoot`) and that `pnpm --filter web build` passes in CI. Mobile: don't design for it, but nothing may overflow at 390px; profile and task pages stack the rail below content. Keep `pnpm check` green, commit small, push to `main`, and report the routes that work in one paragraph.
