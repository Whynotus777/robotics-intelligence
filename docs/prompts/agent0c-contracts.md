# Agent 0c — contract follow-ups from the design reconciliation
You are Agent 0 in `Whynotus777/robotics-intelligence`, doing a small `contract-change:` milestone from reconciling the phase 1 design (`docs/design/phase1-canvas.html`) against `docs/data-contract.md`. All changes are additive; regenerate fixtures in the same commit and keep `pnpm check` green. (1) Add optional `qualifier` (short text such as "EDU", "base · more with hands", "optional") to claims and to every claim projection. (2) Add predicates `HAS_OVERVIEW` (ANALYST prose, any entity) and `HAS_TAGLINE` (ANALYST, markets), and optional `image_url` + `image_credit` on the entity core. (3) Add `HAS_DEPLOYMENT_KIND` (enum FIELD_TRIAL | PILOT | COMMERCIAL) on deployments; add an `intelligence_rail` to the task and market responses (assessment count, evidence by class, last_reviewed, vendor count, deployment count, field-trial count) and an optional `deployment_note` label on maturity-board rows. (4) Add an `entity` filter to `/updates`, and make `pnpm seed` route seeded claims through the 0b approve path so `change_events` are derived at seed time (dated by `valid_from` or `published_at`); Updates and the Explore "What's changing" strip must not be empty after a fresh seed. (5) Add `GET /explore/stack-matrix?lens=` returning columns (lens values), rows (canonical layers), and cells of `{chip, robot_count}` for the challenger Explore. (6) Stack route: add per-layer `competing_technologies` (from COMPETES_WITH); task-route vendors carry their HQ place label. (7) Embodiment lens: give `OTHER_MOBILE` its own region labelled "Field & specialized robots" and fix region order to the design's. (8) Seed additions with evidence: Tesla + Optimus, Ghost Robotics + Vision 60 (LIG Nex1 ownership, THIRD_PARTY MEDIUM), Boston Dynamics Atlas; domains Solar and Oil & gas under Energy and Yard & dock under Warehouse & logistics; rename the "Material movement" domain to "Warehouse". Update `docs/data-contract.md` and `docs/ontology.md`. Report in one paragraph.
