# ADR-0001 Foundation (draft — finalized by Agent 0a)

Decisions fixed by the product brief: Postgres 16 canonical; claims with typed value columns and a predicate registry (no JSON values, no EAV for identity); evidence first-class with ordinal confidence; ANALYST assessments as claims + structured extension; bitemporal claims (valid_from/to, observed_at, last_verified_at) with supersede-not-overwrite; graph as projection, no graph DB; Drizzle + Zod + pnpm workspaces; fixtures generated from real handlers. Agent 0a completes this ADR with the schema as built and any deviations.
