# @ri/viz — Agent 2
Pure visualization components (Explore A: nested territories, Explore B: challenger, Robot MRI, Atlas, Compare). Props are the response types from @ri/api-contracts; no data fetching, no routing. Both Explore concepts must be built against identical fixtures.
