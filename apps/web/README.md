# apps/web — Agent 1
Next.js product shell. Composes components from @ri/viz and data from @ri/fixtures (FixtureProvider) or @ri/api (HttpProvider). Never fetches data inside components. See docs/ownership.md.
